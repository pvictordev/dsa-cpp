
#include <iostream>
#include "utils.h" // import utils.h to assure consistency between method declarations, even though it can seem redundant
#include "math.h"  

void printResult(int value) {
    std::cout << add(value, 10) << std::endl;  
}
